Ingestion System Test File

This is a simple text file used for testing the text parser component.

Purpose:
- Verify that plain text files are correctly decoded
- Ensure metadata extraction works properly
- Validate single-chunk parsing behavior

Content:
The ingestion pipeline should take raw bytes and convert them into structured ParsedChunk objects.
Each chunk should preserve the full text content without modification (except decoding).

Notes:
- Encoding: UTF-8
- Expected parser: parse_text
- Expected output: single ParsedChunk with kind="text"

End of file.