# Project Ingestion System

This repository contains a multi-format file parser for AI ingestion pipelines.

It converts files into structured chunks that can later be used for retrieval-augmented generation (RAG).

---

## Features

- Supports multiple file formats
- Extracts structured chunks from code
- Parses text-based files as single chunks
- Extracts metadata for traceability

---